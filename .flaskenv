# Loaded automatically by the `flask` CLI (via python-dotenv).
# Lets you run `flask db upgrade`, `flask create-admin`, etc. without
# exporting FLASK_APP each time. Contains no secrets — safe to commit.
FLASK_APP=run
