# Core web framework
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Login>=0.6            # session authentication
Flask-WTF>=1.2             # CSRF protection + login/user forms
Flask-Migrate>=4.0         # Alembic schema migrations
Flask-Limiter>=3.5         # login rate limiting
email-validator>=2.0       # WTForms email field validation
python-dotenv>=1.0         # load config (SECRET_KEY, DATABASE_URL) from .env

# GIS import (pure-Python, no system GDAL required)
pyshp>=2.3                 # Shapefile parsing
pyproj>=3.6                # reproject projected Shapefiles (.prj) to WGS84 (bundles PROJ)

# Asset library: file naming, GPS-from-photo, and text search
Pillow>=10.0               # image handling + EXIF GPS extraction
pillow-heif>=0.13          # read iPhone HEIC/HEIF photos (transcoded to JPEG on upload)
pypdf>=4.0                 # extract embedded text from PDFs + merge PDF appendices on export
reportlab>=4.0             # generate pre-plan PDF exports (pure-Python, no system libs)
pytesseract>=0.3           # OPTIONAL image OCR — needs the `tesseract` binary on the
#                            host (auto-detected at runtime; skipped gracefully if absent)

# WSGI server for production self-hosting (dev uses Flask's built-in server)
gunicorn>=21.2

# --- Database drivers (SQLite needs none) ---
PyMySQL>=1.1               # MySQL / MariaDB (e.g. PythonAnywhere) — pure-Python
# psycopg[binary]>=3.1     # PostgreSQL
# GeoAlchemy2>=0.14        # spatial column types for PostGIS

# --- Dev / test ---
pytest>=8.0
